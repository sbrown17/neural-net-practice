export default class Brain {
    constructor(input_nodes, hidden_nodes, ouput_nodes){
        this.input_nodes = input_nodes;
        this.hidden_nodes = hidden_nodes;
        this.ouput_nodes = ouput_nodes;
    }
    //ninput nodes based off environment
}